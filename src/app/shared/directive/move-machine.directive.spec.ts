import { MoveMachineDirective } from './move-machine.directive';

describe('MoveMachineDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveMachineDirective();
    expect(directive).toBeTruthy();
  });
});
